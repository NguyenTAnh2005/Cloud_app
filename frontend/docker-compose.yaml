version: "3.9"

services:
  frontend:
    build: .
    container_name: task-frontend
    restart: unless-stopped
    environment:
      VITE_API_URL: http://localhost:8000
    ports:
      - "5173:80"
